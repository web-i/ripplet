ripple: a toolkit to create modular python web applications
======

`ripple` is a collection of webi middlewares, utilities and web server adaptors.

`ripple` is inspired by clojure's `ring`, perl's `plack` and ruby's `rack`.
